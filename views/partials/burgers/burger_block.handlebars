<li>
	{{burger_name}}

	<button class="change-eat" data-id="{{id}}" data-newEat="{{devoured}}">
		{{#if devoured}}DEVOURED!{{else}}TO BE DEVOURED!{{/if}}
	</button>

	<button class="delete-eat" data-id="{{this.id}}">DEVOURED!</button>
</li>
